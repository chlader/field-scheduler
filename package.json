{
  "name": "field-scheduler",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/api",
    "packages/booker-app",
    "packages/admin-app"
  ],
  "scripts": {
    "dev": "concurrently -n api,booker,admin -c blue,green,magenta \"npm run dev -w packages/api\" \"npm run dev -w packages/booker-app\" \"npm run dev -w packages/admin-app\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/api && npm run build -w packages/booker-app && npm run build -w packages/admin-app"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.0"
  }
}
